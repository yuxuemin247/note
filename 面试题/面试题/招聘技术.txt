1、UGC user Generated  content  用户生成内容 也叫UCC 就是原来用户的以下载为主变成下载和上传并重

2、微服务 它是一种新型的软件架构
把一个大型的单个应用程序拆分为数个甚至数十个的支持微服务（大小没有特别的标准），它可以扩展单个组件而不是整个的引用程序堆栈，从而满足服务等级协议。
比如把一个商城项目拆分为 用户模块、订单模块、商品模块，每个模块就是一个微服务只需要负责自己的业务逻辑，其他不必考虑。每个微服务从开发、测试、运维都是独立的，包括数据库也都是独立的，自己就有一套完整的流程，我们完全可以把它成一个项目来对待，不必依赖于其他模块。可以一个服务java写的，一个服务python写的，靠Restful架构分格统一成一个系统的。微服务之间通过 REST　API或者RPC的方式来调用
采用为微服务可以有效的拆分应用，实现敏捷开发和部署

3、三种云服务模型:
SaaS(软件即服务） software as a service 
PaaS(平台即服务） 	 platform as a service
laaS(基础架构即服务） infrastructure as a service
当您开始考虑将业务转移到云时，了解各种云服务的差异和优势比以往任何时候都更加重要

4、
（1）云服务器
        EC2 elastic compute clound  亚马逊弹性计算云
        ECS elastic compute service  阿里弹性计算云      本质上是向用户提供虚拟机资源，背后核心技术采用KVM

        云计算相对于传统的虚拟机技术，最大的增强当属资源调度的能力。
        比如虚拟机所在的物理机出现了故障了怎么办？
        云服务器提供了自动迁移到其他物理机上，并且将数据恢复到故障前最后一刻状态的额能力，如此一来，云计算未用户屏蔽了物理故障。
（2）云存储
	S3 :simple storage service
	OSS:object storage service
(3)云数据库
	RMDB:MySQL、PostgreSQL
	NoSQL:
	Documents:MongoDB
(4) 云缓存	Redis
(5)消息队列SQS simple Queue Service (亚马逊的）
(6)全文检索：Elastic Search
(7)图引擎服务GES：Graph database (华为云)
7、UUMS 统一用户管理系统

8、feed流
feed是一种给用户持续提供内容的数据形式，是由多个内容提供源组成的资源聚合器，由用户主动订阅信息源并且向用户提供内容。
feed是将用户主动订阅的若干消息源组合在一起形成内容聚合器，帮助用户持续地获取最新的订阅源内容
feed流就是给用户提供感兴趣的内容，不同用户推送的内容不一样。比如某些兴趣订阅类产品。或者大数据判定用户感兴趣的内容，或者购买能力之类来确定用户画像推送。或者推送特定的产品。
瀑布流就是源源不断的给你东西。比如传统资讯产品，新闻网站，对所用用户来说无差别展示各种内容
9、docker使用
   1、找到一个合适的镜像
   2、编写Dockerfile,加入我们的代码，然后bulid镜像
   3、测试镜像
   4、push到远端
   5、服务器pull拉下来，运行容器
容器更加轻量级，启动更快（秒级）
容器可移植性更号
10、K8s容器的集群管理，容器的编排
简化应用部署，自动扩容缩容，服务发现和负载均衡
一个master（主节点负责管理和控制）一群NOde节点(计算节点，工作负载节点，里面是具体的容器）
node节点里由DOcker
pod包含一组容器，是k8s调度的最小单元
pod相当于逻辑主机，每个pod都有自己的IP地址
pod内的容器共享相同的IP和端口空间
11、Django在生产环境写号gunicorn配置文件，让gunicorn去调用DJango
gunicorn.conf.py
workers = 5  #定义同时开启的处理请求的进程数量，根据网站流量适当调整
worker_class = "gevent" #采用gevent库，支持异步处理
bind= "0.0.0.0:8000"
gunicorn启动指令
gunicorn start:app -c gunicorn.conf.py 
