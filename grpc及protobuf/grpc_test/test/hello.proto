//使用哪种protobuf
syntax = "proto3";

//包名
package test;

//服务名，函数名
service Bibili {
  //要写的函数,rpc 是定义的关键字
  rpc helloyu(req) returns (resp){}

}

//定义消息体(参数)
message req {
  string name = 1;
  int32 age = 2;
}

message resp {
  string result =1;
}