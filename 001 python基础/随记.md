1、nginx   大概1万

django程序处理大概并发 1000个

存储层 3000个

8个

2、spu standard product Unit (标准产品单位)  如 ipone x
	  sku stock keeping Unit    库存量单位 如 iphone X 全网通256G

3、消息队列中间件是分布式系统中重要的组件，主要解决 应用的解耦 ，异步任务，流量削峰，消息通信等

使用较多的消息队列有 rabbitMQ 、 kafka 

4、redis 中 zip zincrby zrevrange 使用

​	for a,b,c in zip(as,bs,cs)
​	zincrby  keys  score  name
​	zrevrange keys 0 num-1 WITHSCORES 

5、http协议：
在浏览器输入url之后发生了什么

6、

```
house.__name__   ---> 获取函数名             
house.__doc__    ---> 获取文档注释
im  环信 即时通讯云

七牛云文件上传需要

auto_now_add 创建时间


python的bug和进行静态的代码分析

Pychecker 是一个python代码的静态分析工具，它可以帮助查找python代码的bug，会对代码的复杂度和格式提出警告。
pylint是另外一个工具可以进行coding standard 检查

python中search和match()区别
match()函数只匹配是不是字符串的开始位置，search（）会扫描整个string查找
	
**args 当你不确定你的函数里将要传递多少参数时你可以用 *args .例如,它可以传递任意
数量的参数 : 

**kwargs 关键字参数，它可以解包。
```

7、

​	uwsgi 、 WSGI两个都是 一种通信协议。

​	uWSGI是一个全功能的HTTP服务器，实现了WSGI、uwsgi协议、http协议等。它要做的就是把HTTP协议转化成语言支持的网络协议。比如把http协议转换成WSGI协议，让python使用。

​	Gunicorn：是一个工具，管理Django进程。Gunicorn扮演HTTPserver的角色，HTTPserver只负责网络连接。

```
Web Server NginX
    |
    v
HTTP Server  => 负责 1. 接受、断开客户端请求; 2. 接收、发送网络数据 
    |
    v
  WSGI       => 负责 在 HTTPServer 和 WebApp 之间进行数据转换
    |
    v
Web App .py  => 负责 Web 应用的业务逻辑
```

